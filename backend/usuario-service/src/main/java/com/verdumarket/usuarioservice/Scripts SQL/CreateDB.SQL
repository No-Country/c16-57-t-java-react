-- Si no existe la base de datos, ejecute este SQL.
CREATE DATABASE IF NOT EXISTS usuario_service;

-- En caso de existir la base de datos y tener algún fallo, puede dropearla y volver a crearla
DROP DATABASE IF EXISTS usuario_service;